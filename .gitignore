# Build artifacts and packaged binaries
python_files/annotation tool/build/
python_files/annotation tool/dist/
python_files/annotation tool/*.zip
python_files/annotation tool/**/*.exe
python_files/annotation tool/**/*.pkg

*.pptx

# Python virtual environment & caches
.venv/
**/__pycache__/
*.pyc

# Archived observer-variability results (moved outside the working tree)
/ov_results_archive/

# Office temporary lock files
~$*.docx
~$*.xlsx

# Foundation study: track lightweight X-ray PNG previews under samples/, but never
# the heavy volumes / caches.
foundation_contrastive_diff/feature_cache/
foundation_contrastive_diff/samples/**/*.nii.gz
foundation_contrastive_diff/samples/**/*.pt
foundation_contrastive_diff/samples/**/*.npy
